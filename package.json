{
  "name": "unknown-module-agreement-controls",
  "version": "1.0.2",
  "private": true,
  "description": "Owner-selected local agreement-prompt handling and backed-up downloaded-app launch metadata changes; never changes consent records.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node build.js --check",
    "build": "node build.js"
  },
  "devDependencies": {
    "yauzl": "3.4.0",
    "yazl": "3.3.1",
    "acorn": "8.18.0"
  }
}
